import styles from "../../../styles/EntryForm.module.scss";

const RegisterInfo = () => {
  return <div className={styles.info1}>
    <div>
      <h1>Create An Account</h1>
      <p>Register for a new account and get access to the platform.</p>
    </div>
  </div>
}

export default RegisterInfo;